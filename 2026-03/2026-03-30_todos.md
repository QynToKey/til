# [卒制](https://github.com/QynToKey/HowLongWillItLast) (MVP 後)： TODO 機能

## 0️⃣ 現状

> 完了済み

- todosテーブル作成（マイグレーション済み）
- Todoモデル（`belongs_to :user`、`todo_tags`、`tags` との関連）

> これから必要なもの

- `learning_theme_id` カラムの追加（フェーズ2の要件）
- `belongs_to :learning_theme` の追加
- コントローラー・ルーティング・ビュー

---

## 1️⃣ Todo モデルに `learning_theme_id` カラムを追加

> ### カラムを追加するマイグレーション

```bash
$ docker compose exec web rails g migration AddLearningThemeIdToTodos learning_theme:references
      invoke  active_record
      create    db/migrate/20260330082832_add_learning_theme_id_to_todos.rb
```

- `add_reference` → todosテーブルに参照カラムを追加
- `:learning_theme` → `learning_theme_id`というカラム名になる
- `references` → FK制約が自動で付いてくる

  ⬇️ マイグレーションファイルを生成

```ruby
class AddLearningThemeIdToTodos < ActiveRecord::Migration[8.1]
  def change
    add_reference :todos, :learning_theme, null: false, foreign_key: true
  end
end
```

- `null: false` → NOT NULL制約（ER図の設計通り）
- `foreign_key: true` → DBレベルでFK制約を付与

  ⬇️ 問題ないのでそのまま `db:migrate`

```bash
$ docker compose exec web rails g migration AddLearningThemeIdToTodos learning_theme:references
      invoke  active_record
      create    db/migrate/20260330082832_add_learning_theme_id_to_todos.rb
(base) MacBookAir:HowLongWillItLast milkyshadows$ docker compose exec web rails db:migrate
== 20260330082832 AddLearningThemeIdToTodos: migrating ========================
-- add_reference(:todos, :learning_theme, {:null=>false, :foreign_key=>true})
   -> 0.0379s
== 20260330082832 AddLearningThemeIdToTodos: migrated (0.0381s) ===============
```

> ### `Todo` モデルにアソシエーションを設定

```ruby
# app/models/todo.rb
class Todo < ApplicationRecord
  belongs_to :user
  belongs_to :learning_theme

  has_many :todo_tags, dependent: :destroy
  has_many :tags, through: :todo_tags
end
```

> ### `LearningTheme` モデルにもアソシエーションを追加

```ruby
# app/models/learning_theme.rb
  has_many :todos, dependent: :destroy
```

---

## 2️⃣ `Todo` CRUD 実装

> ### `Todo` 作成機能（テーマに自動紐付け）

#### `todos` を `learning_themes` にネストさせてルーティングに追加

👉 *「テーマ自動紐づけ」なので、`Todo` は `learning_themes` にネストさせる*

```ruby
# config/routes.rb
  resources :learning_themes do
    resources :todos
  end
```

📝 ネストすると、`/learning_themes/:learning_theme_id/todos` というURLになり、どのテーマの `Todo` かがURL上で明確になる。

  ⬇️ 確認

```bash
$ docker compose exec web rails routes | grep todo
      learning_theme_todos GET    /learning_themes/:learning_theme_id/todos(.:format)                                               todos#index
                            POST   /learning_themes/:learning_theme_id/todos(.:format)                                               todos#create
    new_learning_theme_todo GET    /learning_themes/:learning_theme_id/todos/new(.:format)                                           todos#new
  edit_learning_theme_todo GET    /learning_themes/:learning_theme_id/todos/:id/edit(.:format)                                      todos#edit
        learning_theme_todo GET    /learning_themes/:learning_theme_id/todos/:id(.:format)                                           todos#show
                            PATCH  /learning_themes/:learning_theme_id/todos/:id(.:format)                                           todos#update
                            PUT    /learning_themes/:learning_theme_id/todos/:id(.:format)                                           todos#update
                            DELETE /learning_themes/:learning_theme_id/todos/:id(.:format)                                           todos#destroy
```

#### `Todos` コントローラーを作成

```bash
$ docker compose exec web rails g controller todos
      create  app/controllers/todos_controller.rb
      invoke  erb
      create    app/views/todos
      invoke  helper
      create    app/helpers/todos_helper.rb
```

```ruby
# app/controllers/todos_controller.rb
class TodosController < ApplicationController
  before_action :set_learning_theme
  before_action :set_todo, only: %i[edit update destroy]

  def index
    @todos = @learning_theme.todos.order(created_at: :desc)
  end

  def new
    @todo = @learning_theme.todos.build
  end

  def create
    @todo = @learning_theme.todos.build(todo_params)
    @todo.user = current_user

    if @todo.save
      redirect_to learning_theme_todos_path(@learning_theme), notice: "Todoを保存しました"
    else
      flash.now[:alert] = "Todoの保存に失敗しました"
      render :new, status: :unprocessable_entity
    end
  end

  def edit
  end

  def update
    if @todo.update(todo_params)
      redirect_to learning_theme_todos_path(@learning_theme), notice: "Todoを更新しました"
    else
      flash.now[:alert] = "Todoの更新に失敗しました"
      render :edit, status: :unprocessable_entity
    end
  end

  def destroy
    @todo.destroy
    redirect_to learning_theme_todos_path(@learning_theme), notice: "Todoを削除しました"
  end

  private

  def todo_params
    params.require(:todo).permit(:title, :description, :is_completed)
  end

  def set_learning_theme
    # current_user の learning_themes の中からのみ検索することで、他ユーザーの learning_theme にアクセスできないようにする
    @learning_theme = current_user.learning_themes.find(params[:learning_theme_id])
  end

  def set_todo
    # @learning_theme の todos の中からのみ検索することで、他ユーザーの todo にアクセスできないようにする
    @todo = @learning_theme.todos.find(params[:id])
  end
end
```

- `set_learning_theme` を全アクションで実行 *（ネストしているため）*
- `create` で `@todo.user = current_user` を明示的にセット *（`learning_theme` 経由で build するため `user_id` が自動でセットされない）*

---

> ### ビューを実装
