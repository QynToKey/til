# [卒制](https://github.com/QynToKey/HowLongWillItLast) (day 15)：Tags ビューの実装

---

## ビューの実装

### 1️⃣ 一覧ページ

```bash
touch app/views/tags/index.html.erb
```

```ruby
<h1>タグ一覧</h1>

<%= link_to 'タグを追加する', new_tag_path %>

<table>
  <thead>
    <tr>
      <th>タグ名</th>
    </tr>
  </thead>
  <tbody>
    <% @tags.each do |tag| %>
      <tr>
        <td><%= tag.name %></td>
        <td>
          <%= link_to '編集', edit_tag_path(tag) %>
          <%= link_to '削除', tag_path(tag), data: { turbo_method: :delete, turbo_confirm: '本当に削除しますか？' } %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
```

---

### 2️⃣ 作成ページ

```bash
touch app/views/tags/new.html.erb
```

```ruby
<h1>タグの作成</h1>

<% if @tag.errors.any? %>
  <div style="color: red;">
    <h2><%= @tag.errors.count %>件のエラーがあります</h2>
    <ul>
      <% @tag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<div>
  <%= form_with model: @tag, do |f| %>
    <div>
      <%= f.label :name, "タグ名" %><br>
      <%= f.text_field :name %>
    </div>

    <div>
      <%= f.submit "作成する" %>
      <%= link_to "戻る", tags_path %>
    </div>
  <% end %>
</div>
```

---

### 3️⃣ 編集ページ

```bash
touch app/views/tags/edit.html.erb
```

```ruby
<h1>タグの編集</h1>

<% if @tag.errors.any? %>
  <div style="color: red;">
    <h2><%= @tag.errors.count %>件のエラーがあります</h2>
    <ul>
      <% @tag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<div>
  <%= form_with model: @tag do |f| %>
    <div>
      <%= f.label :name, "タグ名" %><br>
      <%= f.text_field :name %>
    </div>

    <div>
      <%= f.submit "更新する" %>
      <%= link_to "戻る", tags_path %>
    </div>
  <% end %>
</div>
```

---

### 4️⃣ 共通部分をパーシャル化

```bash
touch app/views/tags/_form.html.erb
```

```ruby
<div>
  <%= form_with model: @tag do |f| %>
    <div>
      <%= f.label :name, "タグ名" %><br>
      <%= f.text_field :name %>
    </div>

    <div>
      <%# f.submit のラベルはi18n対応後に削除予定 %>
      <%= f.submit tag.new_record? ? "記録する" : "更新する" %>
      <%= link_to "戻る", return_path %>
    </div>
  <% end %>
</div>
```

⬇️ `new` と `edit` の当該箇所を以下と差し替え

```ruby
<%= render 'form', tag: @tag, return_path: tags_path %>
```

---

### 5️⃣ ヘッダーに「タグ管理」を追加

```ruby
  <nav>
    <% if logged_in? %>
      <%= link_to "HOME", root_path %> |
      <%= link_to "マイページ", edit_user_path(current_user) %> |
      <%= link_to "タグ管理", tags_path %> |
      <%= link_to "ログアウト", logout_path, data: { turbo_method: :delete } %>
    <% else %>
      <%= link_to "HOME", root_path %> |
      <%= link_to "ログイン", login_path %> |
      <%= link_to "新規登録", new_user_path %>
    <% end %>
  </nav>
```

---

### 6️⃣ LearningRecords に「タグ」を追加

```ruby
# app/views/learning_records/_form.html.erb
  <div>
    <%= f.label :tag_ids, "タグ" %><br>
    <% current_user.tags.each do |tag| %>
      <%= f.check_box :tag_ids, { multiple: true, checked: learning_record.tags.include?(tag) }, tag.id, false %>
      <%= tag.name %>
    <% end %>
  </div>
```

📝 `<%= f.check_box :tag_ids, { multiple: true, checked: learning_record.tags.include?(tag) }, tag.id, false %>`

- `tag_ids` ：送信するパラメータの名前。`learning_record[tag_ids][]` という形でサーバーに送られる
- `multiple: true` ：複数のチェックボックスから配列として値を送信するための指定
- `checked: learning_record.tags.include?(tag)` ：編集時に「すでにこのタグが付いているか」を確認して、チェック済み状態にするかどうかを決める *（新規作成時は `false`）*
- `tag.id` ：チェックされたときに送信される値 *（どのタグが選ばれたかをIDで識別する）*
- `false` ：チェックされていないときに送信される値  *（通常は `0` や `false` を指定して「未選択」を明示する。 `false` のとき、未チェック時は何も送信されない）*
