# [卒制](https://github.com/QynToKey/HowLongWillItLast) (day 18)：全体調整

## 0️⃣ 今日の Issues

- 全体調整 : 6sp
  - TOP ページ他の仕上げ : 2sp
    - 日付から当該日の学習ログ (詳細画面) へ遷移する（`index` / `edit` ページ）
    - 未ログインユーザーの動線を配置（TOP ページ）
    - サイトの説明等（TOP ページ）
    - フィードバックリンクの配置（TOP ページ）
  - i18n 設定 : 2sp
    - 各種ラベル / ボタンなど
  - CSS デザイン : 2sp
    - 全体的なレイアウト調整
    - レスポンシブ対応

---

## 1️⃣ TOP ページ他の仕上げ

### 日付から当該日の学習ログ (詳細画面) へ遷移する

**実装意図：**
「カレンダー機能」の実装は MVP 後に先送りしたが、初期構想段階にあった最低限の機能は実装しておきたい。

#### □ `index` ページの「日付」に「一覧ページ」へのリンクを追加

```ruby
# app/views/learning_records/index.html.erb
<td><%= link_to record.study_date.strftime('%Y-%m-%d'), learning_records_path(date: record.study_date) %></td>
```

👉 *`learning_records_path()` に `record.study_date` を渡す*

#### □ `LearningRecordsController` の `index` アクションに条件分岐を追加

```ruby
# app/controllers/learning_records_controller.rb
  def index
    ・・・

    if params[:tag_id].present?
      # ユーザーが所有するタグの中から、指定されたタグIDに該当するタグを見つける
      @current_tag = current_user.tags.find(params[:tag_id])
      # タグIDが指定されている場合は、そのタグが付いている学習記録のみを表示する
      @learning_records = @learning_records.joins(:tags).where(tags: { id: @current_tag.id })
    elsif params[:date].present?
      # 学習日が指定されている場合は、その日に該当する学習記録のみを表示する
      @learning_records = @learning_records.where(study_date: params[:date])
    end
  end
```

#### □ `user.rb` に `total_learning_minutes_by_date()` メソッドを追加

```ruby
  # 指定した日付の学習時間の合計を計算する
  def total_learning_minutes_by_date(date)
    learning_records.where(study_date: date).sum(:duration_minutes)
  end
```

#### □ `index` ページの絞り込み表示を修正

```erb
<%# 絞り込み条件によって見出しを変える %>
<% if params[:tag_id].present? %>
  <h1>'<%= @current_tag.name %>' の学習ログ</h1>
<% elsif params[:date].present? %>
  <h1>'<%= Date.parse(params[:date]).strftime('%Y/%m/%d') %>' の学習ログ</h1>
<% else %>
  <h1>学習ログ</h1>
<% end %>
```

```erb
<%# 日付で絞り込んでいる場合は全件表示へのリンクを表示する %>
<% elsif params[:date].present? %>
  <p>'<%= Date.parse(params[:date]).strftime('%Y/%m/%d') %>' の学習時間累計： <%= (current_user.total_learning_minutes_by_date(Date.parse(params[:date])) / 60.0).round(1) %> 時間</p>
  <%= link_to "全件表示へ戻る", learning_records_path %>
```

---

### 未登録ユーザーのための動線を追加

**実装方針：**
未ログインでも `new`（記録フォーム＋ストップウォッチ）は使えるが、保存しようとしたらログインを促す。

#### □ TOP ペ ージに主要なページへの動線を追加 `app/views/home/index.html.erb`

```html
<h1>How Long Will It Last</h1>

<p>このアプリは、あなたの学習時間を記録し、どれくらい続けられるかを可視化するためのツールです。</p>

<p>学習記録を追加して、あなたの学習時間を管理しましょう！</p>

<%= link_to '今日の記録', new_learning_record_path(study_date: Date.current) %> |
<%= link_to '学習記録一覧', learning_records_path %> |
<%= link_to '試してみる', new_learning_record_path %>
```

#### □ 未ログイン ユーザーの動線を追加 `app/views/learning_records/_form.html.erb`

```html
  <div>
    <% if logged_in? %>
      <%= f.label :tag_ids, "タグ" %><br>
      <% if current_user.tags.empty? %>
        <p>※ タグを先に<%= link_to 'タグ管理画面', tags_path %>から追加してください。</p>
      <% else %>
        <% current_user.tags.each do |tag| %>
          <%# タグがすでに付いていればチェック済みで表示し、チェックされたらそのIDを送信する %>
          <%= f.check_box :tag_ids, { multiple: true, checked: learning_record.tags.include?(tag) }, tag.id, false %>
          <%= tag.name %>
        <% end %>
      <% end %>
    <% else %>
      <p>※ 記録するにはユーザー登録 / ログインしてください。</p>
      <%= link_to 'ユーザー登録', new_user_path %> |
      <%= link_to 'ログイン', login_path %>
    <% end %>
  </div>

  <div>
    <% if logged_in? %>
      <%# f.submit のラベルはi18n対応後に削除予定 %>
      <%= f.submit learning_record.new_record? ? "記録する" : "更新する" %>
      <%= link_to "戻る", return_path %>
    <% end %>
  </div>
```

👉 *`if logged_in?` で表示 / 非表示を条件分岐*
