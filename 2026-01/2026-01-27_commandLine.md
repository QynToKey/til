## あらためてコマンドラインの復習など

- `CUI` または `CLI`: **コマンド**(文字)入力によって操るインターフェイスのこと。*(Character-based User Interface / Command-Line Interface)*

### Tips

  - `date`: 現在時刻
  - `cal`: カレンダー

  - `TABキー`: 多くの環境では、1回押すと文字列が**補完**される／2回押すと**補完候補の一覧**が表示される

  - `Ctrl + L` or `clear`: 画面表示をクリアする
  - `Ctrl + U`: 入力中のコマンドをクリアする
  - `Ctrl + C`: フォアグラウンドで実行中のプロセスを中止する

  - `--help`: 使用可能な**オプション**が一覧表示される
  - `man コマンド`: そのコマンドの**マニュアル**を表示


### ディレクトリの操作

  - `pwd`: 現在居るディレクトリ *(Present Working Directory)*
  - `cd ディレクトリのパス`: 指定したディレクトリへ移動 *(Change Directory)* 🤛 *たんに`cd`とすると`ホームディレクトリ`へ移動する*

  - `mkdir ディレクトリ名`: ディレクトリに名前をつけて生成 *(Make Directory)*
  - `mv ファイル名 ディレクトリ名/`: 指定したファイルを指定したディレクトリ下へ移動 *(Move)*
  - `mv 変更前のディレクトリ名/ 変更後のディレクトリ名`: ディレクトリ名を変更
  - `rmdir ディレクトリ名`: 指定したディレクトリを削除 *(Remove Directory)*
  ⚠️ *ディレクトリが空でない場合は中にあるファイルを移動させてから実行するか、`rm -r`で中のファイルごと削除する*

  - `mkdir -p 親となるディレクトリ名/子となるディレクトリ名`: 深い階層にディレクトリを生成する 🤛 *(parents)*
  - `cp -r 元のディレクトリ名 作りたいディレクトリ名`: ディレクトリの中にあるファイルも含めてコピーして生成する *(Recursive)*

  #### 絶対パス
    | コマンド表記 | 対応するディレクトリ |
    | --- | --- |
    | `/` | ルートディレクトリ |
    | `~` | ホームディレクトリ |

  #### 相対パス
    | コマンド表記 | 対応するディレクトリ |
    | --- | --- |
    | `.` | 現在のディレクトリ |
    | `..` | ひとつ上のディレクトリ |
    | `../..` | ふたつ上のディレクトリ |
    | `../../etc` | ふたつ上に遡って、その下の階層のディレクトリを指すとき |

  #### シンボリックリンク
    - `ln -s ディレクトリやファイルの絶対パス リンク名`: 頻繁にアクセスするディレクトリやファイルに、**エイリアス**のような機能を設定できる *(`ln`: Link / `-s`: Symbolic)*
    - `rm シンボリックリンク名`: 「シンボリックリンクを設定した**ファイル**」を削除
    - `rm -r シンボリックリンク名`: 「シンボリックリンクを設定した**ディレクトリ**」を削除


### ファイルの操作

  - `touch ファイル名`: ファイルに名前をつけて生成
  - `cat ファイル名`: ファイルの中身を取得 *(Catch)*
  - `cp 元のファイル名 作りたいファイル名`: ファイルをコピーして生成 *(Copy)*
  - `mv 変更前のファイル名 変更後のファイル名`: ファイル名を変更
  - `rm ファイル名`: 指定したファイルを削除

  #### ファイル内の検索

  - `wc ファイル名`: 「行数 単語数 バイト数 ファイル名」を表示 *(Word Count)* ⚠️ *日本語テキストの場合、単語の区切りが不明確なため正確な単語数は表示できない*
    - `wc -l`: 「行数 ファイル名」のみを取得 *(Line)*
  - `head -行数 ファイル名`: 先頭から「指定した行数」だけを取得
  - `tail -行数 ファイル名`: 最後の「指定した行数」だけを取得
  - `grep '単語' ファイル名`: 「検索したい単語」が含まれる**行**を表示


  #### ページャ (`more`, `less')

    - `more ファイル名`: 長大なファイルの一部を、画面高に収まる部分だけ表示する *(全体の何%が表示されているかが下端に表示される)*
      - `Spaceキー`: **次ページ**を表示
      - `q`: ページャを終了

    - `less ファイル名`: `more`と同じく長大なファイルの一部を表示することに加え、以下の操作が可能
      - `/検索したい単語`: `:`の箇所に入力するとファイル内を**単語検索**し、その**箇所まで移動**する
      - `n`: 次の検索結果まで移動する *(Next)*


### 検索のいろいろ

  #### ワイルドカード （`*`, `?`, `[]`, `{}`）

  - 例）`*文字列`: 任意の文字列(`.`を含む)で始まり、その文字列で終わる項目を検索して表示
  - 例）`s?????`: `s`で始まる6文字の項目を検索して表示
  - 例）`{ps}?????`: `p`または`s`で始まる6文字の項目を検索して表示
  - 例）`[f-h]*`: `f`から`h`の範囲(`-`)の文字を頭文字とする項目を検索して表示
  - 例）`{sh,ho}*`: `sh`または`ho`で始まる項目を検索して表示 ⚠️ *`,`の後に「空白」を入れない！*
