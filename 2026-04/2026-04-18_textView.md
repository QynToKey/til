# [co-READER](https://github.com/QynToKey/co_reader)（day: 10）： Text 表示

## 0️⃣ 実装内容

一般ユーザーがテキストを読むためのページを作成。

---

## 1️⃣ ルーティングに `texts` を追加

```ruby
# config/routes.rb
  resources :texts, only: %i[ show ]
```

---

## 2️⃣ `Texts` コントローラーを作成

```bash
touch app/controllers/texts_controller.rb
```

```ruby
# app/controllers/texts_controller.rb

---

## 3️⃣ `texts/show` ビューを作成

```bash
mkdir -p app/views/texts/ && touch app/views/texts/show.html.erb
```

```erb
<div class="container mt-5" style="max-width: 1000px;">
  <h1 class="h4 mb-2"><%= @text.title %></h1>

  <p class="text-muted small mb-4">
    <%= @text.created_at.strftime("%Y/%m/%d") %>
  </p>

  <div class="lh-lg">
    <%= simple_format(@text.body) %>
  </div>
</div>
```

---
