# [co-READER](https://github.com/QynToKey/co_reader)（day: 4）： Sorcery 設定

## 1️⃣ Sorcery 導入

> `Gemfile` に追加

```Ruby
# Gemfile
gem "sorcery"
```

> コマンドを実行

```bash
$ docker compose exec web bundle install
$ docker compose exec web rails g sorcery:install
      create  config/initializers/sorcery.rb
    generate  model User --skip-migration
        rails  generate model User --skip-migration
      invoke  active_record
      create    app/models/user.rb
      insert  app/models/user.rb
      create  db/migrate/20260410072402_sorcery_core.rb
```

> `remember_me` サブモジュールを有効化

```ruby
# config/initializers/sorcery.rb
Rails.application.config.sorcery.submodules = [:remember_me]
```

> `remember_me` 用のマイグレーションファイルを生成

```bash
$ docker compose exec web rails generate sorcery:install remember_me
  --migrations-only     conflict  config/initializers/sorcery.rb
Overwrite /app/config/initializers/sorcery.rb? (enter "h" for help) [Ynaqdhm] n
        skip  config/initializers/sorcery.rb
        gsub  config/initializers/sorcery.rb
    generate  model User --skip-migration
       rails  generate model User --skip-migration
      invoke  active_record
The name 'User' is either already used in your application or reserved by Ruby on Rails. Please choose an alternative or use --skip-collision-check or --force to skip this check and run this generator again.
   unchanged  app/models/user.rb
   identical  db/migrate/20260410072402_sorcery_core.rb
      create  db/migrate/20260410072826_sorcery_remember_me.rb
```

> マイグレーションを実行

```bash
$ docker compose exec web rails db:migrate
== 20260410072402 SorceryCore: migrating ======================================
-- create_table(:users)
   -> 0.0397s
== 20260410072402 SorceryCore: migrated (0.0398s) =============================

== 20260410072826 SorceryRememberMe: migrating ================================
-- add_column(:users, :remember_me_token, :string, {:default=>nil})
   -> 0.0026s
-- add_column(:users, :remember_me_token_expires_at, :datetime, {:default=>nil})
   -> 0.0017s
-- add_index(:users, :remember_me_token)
   -> 0.0030s
== 20260410072826 SorceryRememberMe: migrated (0.0074s) =======================
```

---

## 2️⃣ User モデルのバリデーション

```ruby
# app/models/user.rb
class User < ApplicationRecord
  authenticates_with_sorcery!

    validates :email, presence: true, uniqueness: true,
                    format: { with: URI::MailTo::EMAIL_REGEXP }
    validates :password, length: { minimum: 8 }, if: -> { new_record? ||
  changes[:crypted_password] }
    validates :password_confirmation, presence: true, if: -> { new_record?
  || changes[:crypted_password] }
end
```

👉 *`password` は仮想属性（Sorcery が自動生成）。`crypted_password` が変わったタイミングだけ検証する*
👉 *`URI::MailTo::EMAIL_REGEXP` は Ruby 標準ライブラリのメール形式チェック用正規表現*

---
